CREATE DATABASE SPRINT1;

-- SELECIONAR BANCO DE DADOS. 
USE SPRINT1;

-- INSERIR DADOS NA TABELA.
-- CRIAR A TABELA ALUNO
CREATE TABLE atleta (idatleta int primary key,
nome varchar(40),
modalidade varchar(40),qtdmedalha int);
 
-- INSERIR DADOS NA TABELA.
insert into atleta values 
('118', 'victor', 'ciclismo', '6'),
('119', 'wallace', 'ginástica', '8'),
('120', 'vitor', 'box', '9'),
('121', 'marcos', 'nataçao', '12'),
('122', 'maria', 'nataçao', '11'),
('123', 'joao', 'nataçao', '5'),
('124', 'emerson', 'box', '1'),
('125', 'pandora', 'nataçao', '14');


 -- Exibir todos os dados da tabela.
 select * from atleta;
 
 -- Exibir os nomes e quantidade de medalhas dos atletas.
 select nome, qtdmedalha from atleta;
 
 -- exibir dados dos nadadores.
 select *  from atleta 
 where modalidade = 'nataçao';
 
 -- Exibir os dados da tabela, ordenados pela modalidade.
 select modalidade,qtdmedalha,idatleta from atleta;
 
 -- Exibir os dados da tabela,ordenados pela quantidade de medalhas em ordem decrescente. (<).
 select * from atleta order by qtdmedalha desc; 
 
 -- Exibir os dados da tabela dos atletas, cujo nome contenham a letra (S).
 select * from atleta
	where nome like '%s';
 
 -- Exibir os dados da tabela dos atletas, cujo nome comece com uma determinada letra (M).
 select * from atleta
	where nome like 'm%';
    
-- Exibir os dados da tabela dos atletas, cujo nome termine com a letra (O).
select * from atleta
	where nome like '%o';
    
-- Exibir os dados da tabela dos atletas, cujo nome tenham a penúltima letra (R).
select * from atleta
    where nome like '%r_';
    
-- Eliminar a tabela. 
drop table atleta;
    
    
-- EXERCÍCIO 2 

-- SELECIONAR O BANCO DE DADOS. 
USE SPRINT1;


-- CRIAR A TABELA musica 
CREATE TABLE musica (idmusic int primary key,
titulo varchar(40),
artista varchar(40),genero int);
 
 
-- INSERIR DADOS NA TABELA.
insert into musica values 
('1415', 'encomenda', 'leall', 'trap'),
('1416', 'pixadao', 'felipe ret', 'trap'),
('1417', 'FFM', 'felipe ret', 'trap'),
('1418', 'melhor fase', 'caio lucas', 'trap'),
('1419', 'joao e maria', 'chico buarque', 'mpb'),
('1420', 'flor de maracuja', 'alceu valença', 'mpb'),
('1421', 'morena tropicana', 'alceu valença', 'mpb'),
('1422', 'doja', 'centralcee', 'rapp');

-- Exibir todos os dados da tabela.
select * from musica;

-- Exibir apenas os titulos e os artistas das musica.
select titulo, artista from musica;

--  Exibir apenas os dados das músicas de um determinado gênero.
select * from musica 
where genero = 'mpb';
  
  -- Exibir apenas os dados das músicas de um determinado artista.
select titulo,artista,genero,idmusica from musica;
  
 -- Exibir os dados da tabela ordenados pelo artista em ordem decrescente. (<)
select * from musica order by artista  desc;

-- Exibir os dados da tabela das músicas,cujo título comece com a letra (F)
 select * from musica
	where titulo like 'f%';

-- Exibir os dados da tabela de músicas cujo o artista termine com  a letra (A)
select * from musica
	where titulo like '%a';
    
    
-- Exibir os dados da tabela das músicas, cujo gênero tenha o (P) como segunda letra. 
select * from musica
where genero like '_p%';
    
    
-- Exibir os dados das musicas, cujo os títulos tenham as mesma penúltima letra (p)
    
select * from musica
	where titulo like 'f%_';


-- elime a tabela 
    drop table musica;
    
    
    
    -- EXERCÍCIO 3 

-- SELECIONAR O BANCO DE DADOS. 
USE SPRINT1;


-- CRIAR A TABELA FILME  
CREATE TABLE FILME (iDFILME int primary key,
titulo varchar(50),
genero varchar(40),diretor varchar(40));

 
-- INSERIR DADOS NA TABELA.
insert into filme values 
('58', 'AVATAR', 'AÇAO','STEVEN SPIELBERG'),
('59', 'Indiana Jones e o Reino da Caveira de Cristal', 'AÇAO','JORGE LUCAS'),
('60', 'STAR WARS', 'FICÇAO CIENTIFICA', 'JORGE LUCAS'),
('61', 'TITANIC', 'DRAMA', 'STEVEN SPIELBERG'),
('62', 'A FERA DO MAR', 'ANIMAÇAO', 'CHRIS WILLIAMS'),
('63', 'A CAMINHO DA LUA', 'ANIMAÇAO', 'GLEN KEANE'),
('64', 'KLAUS', 'ANIMÇAO', 'SERGIO PABLOS');



-- Exibir todos os dados da tabela.
select * from FILME;

-- • Exibir apenas os títulos e os diretores dos filmes.
select titulo,DIRETOR FROM FILME;

-- • Exibir apenas os dados dos filmes de um determinado gênero.
select * from FILME
  where genero = 'AÇAO';
  
-- Exibir apenas os dados dos filmes de um determinado diretor..
  select * from FILME
  where diretor = 'STEVEN SPIELBERG';
  
-- Exibir os dados da tabela ordenados pelo artista em ordem decrescente. (<)
 select * from filme order by diretor  desc;
 
 
--  Exibir os dados da tabela ordenados pelo título do filme
select titulo,DIRETOR,idfilme FROM FILME;

-- Exibir os dados da tabela ordenados pelo diretor em ordem decrescente
select * from filme order by diretor  desc;

-- Exibir os dados da tabela de músicas cujo o artista termine com  a letra (A)
select * from musica
	where titulo like '%a';
    
    
-- Exibir os dados da tabela das músicas, cujo gênero tenha o (P) como segunda letra. 
    select * from musica
	where genero like '_p%';
    
    
-- Exibir os dados das musicas, cujo os títulos tenham as mesma penúltima letra (p)
    
     select * from musica
	where titulo like 'f%_';
    
-- elime a tabela 
    drop table musica;
    
    
-- Exibir os dados da tabela cujo título do filme se inicie com a letra  (S)
     select * from filme
	where titulo like 's%';
     
--  Exibir os dados da tabela cuja segunda letra seja  (T)
     
	select * from filme
	where titulo like '_t%';
    
-- Exibir os dados dos filmes, cujo o título tenha a penúltima letra 
     select * from filme
	where titulo like '%a_';
    
-- elime a tabela 
    drop table filme;
    
    
    
-- EXERCÍCIO 4

-- SELECIONAR O BANCO DE DADOS. 
USE SPRINT1;


-- CRIAR A TABELA FILME  
CREATE TABLE professor (idprofessor int primary key,
nome varchar(50), especialidade varchar(40), dtnasci date);

 
-- INSERIR DADOS NA TABELA.
insert into professor values 
('2936', 'vera', 'algoritimo','1980-11-03'),
('2937', 'monica', 'gestao empresarial','1970-09-07'),
('2938', 'vivian', 'banco de dados', '1985-08-13'),
('2939', 'fernando', 'pesquisa e inovaçao', '1982-04-29'),
('2940', 'thiago', 'psicologia', '1989-11-28'),
('2941', 'caio', 'algoritimo', '1993-11-29'),
('2942', 'eduardo', 'arq computacinal', '1989-12-09');




-- Exibir todos os dados da tabela.
select * from professor;



-- • Exibir apenas as especialidades dos professores
select especialidade FROM professor;



-- • Exibir apenas os dados dos professores de uma determinada especialidade.
select * from professor
  where especialidade = 'algoritimo';
  
-- Exibir apenas os dados dos filmes de um determinado professor 
  select nome,especialidade,dtnasci from professor;
 
 
  -- Exibir os dados da tabela ordenados pela data de nascimento do professor em ordem decrescente.
 select * from professor order by dtnasci  desc;
 
 
 
 -- Exibir os dados da tabela cujo nome do professor se inicie com a letra  (V)
     select * from professorr
	where nome like 'v%';select nome,especialidade,idprofessor FROM professor;



-- Exibir os dados da tabela, dos professores cujo nome termine com uma determinada letra
select * from professor 
    where nome like '%a';
    
    
-- Exibir os dados da tabela dos professores, cujo nome tenha o (E) como segunda letra. 
select * from professor
	where nome like '_e%';
    
    
- -- Exibir os dados da tabela dos professores, cujo o nome  tenha como a penúltima o letra (R)
    select * from professor
	where nome like '%r_';
    
    
-- Exibir os dados das musicas, cujo os títulos tenham as mesma penúltima letra (p)
    

-- elime a tabela 
    drop table professor;
    
    
    
-- EXERCÍCIO 5

-- SELECIONAR O BANCO DE DADOS. 
USE SPRINT1;


-- CRIAR A TABELA CURSO
CREATE TABLE curso (idcurso int primary key,
nome varchar(50), sigla char(3), coordenador varchar(40));

 
-- INSERIR DADOS NA TABELA.
insert into curso values 
('01', 'tinturaria', 'ttr','renato'),
('02', 'logistica', 'lgt','marcela'),
('03', 'mecanico', 'mcn', 'adriano'),
('04', 'direito', 'drt', 'alex'),
('05', 'medicina', 'mdc', 'julia'),
('06', 'enfermagem', 'efm', 'vitor'),
('07', 'caldeireiro', 'cdr', 'julio');




-- Exibir todos os dados da tabela.
select * from curso;



-- • Exibir apenas os coordenadores dos cursos
select coordenador FROM curso;


-- • Exibir apenas os dados dos cursos com a letra (C)
select * from curso
  where nome = 'caldeireiro';
  
-- Exibir os dados da tabela ordenados pelo nome do curso
  select nome,coordenador,idcurso from curso;
 
 
 
-- Exibir os dados da tabela ordenados pelo nome do coordenador em ordem decrescente

 select * from curso order by coordenador desc;

 
 -- Exibir os dados da tabela cujo nome do coordenador se inicie com a letra  (V)
   
   select * from curso
     where curso like 'v%';select nome,coordenador,idcurso FROM curso;



-- Exibir os dados da tabela, do coordenador cujo nome termine com a letra (R)
select * from curso
    where coordenador like '%r';
    
    
-- Exibir os dados da tabela dos coordenadores, cujo nome tenha o (U) como segunda letra. 
select * from curso
	where coordenador  like '_u%';
    
    
- -- Exibir os dados da tabela curso cujo os coordenadores  tenham como a penúltima o letra (T)
    select * from curso
	where coordenador like '%t_';
    
    
-- elime a tabela 
    drop table curso; 
    
    
    

    
   
    